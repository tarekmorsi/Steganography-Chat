var fs = require('fs');

export default {

  async encode (text) {
    // hide text in image
    var image = '';

    // upload image

    return link
  },

  async decode (link) {
    // get image from link

    //decode text from image
    var text = '';

    return text;
  }

}
